{
  "name": "monaco-editor-esm",
  "version": "1.0.0",
  "description": "monaco editor esm build",
  "main": "index.js",
  "scripts": {
    "build": "vite build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "monaco-editor": "^0.29.1",
    "vite": "^2.6.10"
  }
}
